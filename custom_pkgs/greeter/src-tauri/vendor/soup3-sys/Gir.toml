[options]
library = "Soup"
version = "3.0"
target_path = "."
min_cfg_version = "3.0"
work_mode = "sys"
single_version_file = "."
girs_directories = [ "..", "../gir-files/" ]
external_libraries = [
   "GLib",
   "GObject",
   "Gio",
]

ignore = [
  "Soup.MICRO_VERSION",
  "Soup.MINOR_VERSION",
  "Soup.VERSION_MIN _REQUIRED",
]
